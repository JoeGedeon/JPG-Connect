[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/fleetflow_v9.html"
  status = 200

[context.production]
  environment = { NODE_ENV = "production" }
